// index.js
window.onload =function(){

    const oneScroll = document.querySelector('main'),
        container = document.querySelectorAll('section'),
        lastPage = container.length - 1;
        let page = 0;

        window.addEventListener('wheel',(event)=>{
            
        })
}
